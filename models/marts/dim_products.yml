version: 2

models:
  - name: dim_products
    description: The products table for our dw.
    columns:
      - name: product_sk
        description: The primary key of the product.
        tests:
          - unique
          - not_null

      - name: product_id